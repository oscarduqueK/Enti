#include <cstdio>

//Sonic case: write the code when Sonic is injured (lives, coins, score)
/*
int main()
{
	int score = 33; 
	int lives = 1;
	int rings = 0;
	bool hit = true;

	//int rest = rings- rings; 
	//int resta = lives - 1;
	//int resto = score - 5;

	if (rings > 0)
	{
		rings = 0;
		printf("te han golpeado, ara tienes %d anillos\n", rings);
	}

	else
	{
		//lives = lives - 1;
		lives = lives--;
		score = score - 10;
		printf("te han golpeado, ara tienes %d vidas y %d puntos\n", lives, score);
			if(lives == 0)
				printf("Game Over\n"); 
	}
} */

//Mario case: write the code when Mario gets a coin (lives, coins, score)
/*
int main()
{
	int lives = 4;
	int coins = 100;
	int score = 100;
	bool pickcoin = true;
	
	if (pickcoin = true)
	{
		coins = coins++;
		printf("+1 coin\n");
			
			if (coins = 100)
				{
					lives = lives++;
					coins -= coins;
					printf("Has llegado a 100 monedas, +1 vida, tus vidas son %d, tus monedas ahora son %d\n", lives, coins);
				}
		
	}

	
}*/

//CSGO case: write the code when a player gets injured by 30 supposing there is no penetration (stamina, armour)
/*
int main()
{
	int live = 100;
	int armour = 50;
	int daño = 30;
	bool gethit = true;



	if (gethit == true)
	{
		if (armour >= 1)
		{
			(armour = armour - daño) && (live = live - daño * 0.5);
			printf("ara tu vida es %d y tu armadura a %d\n", live, armour);
		}


	}


} */

