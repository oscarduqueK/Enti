#include <cstdio>

// Check whether a player has talked with an NPC called Raistlin and he has either more than 30 coins or not talked with an NPC called Mira(talkedRaistlin, coins, talkedMira)
/*
int main()
{
	bool tr = true;				// talked raisltin
	bool tm = true;				// talked mira
	int ct = 20;				//30 coins 
	bool mc = false;			//mision completed


	if (tr == true && (ct >= 30 || tm == false))
	{
		bool mc = true;
			printf("la mision esta completada\n"); 
	}
	else
	{
		bool mc = false;
		printf("la mision esta incompleta\n");

	}
}*/

//Check whether a player has talked with either Raistlin or Mira but not with both(talkedRaistlin, talkedMira)
/*
int main()
{
	bool tr = true;				// talked raisltin 
	bool tm = false;			// talked mira
	bool mcc = false;			//talekd with one but not both

	if (tr == true || tm == true)
		printf("mision completa\n");

	else if (tr == true && tm == false)
		printf("mision fallida\n");

	else
		printf("u r useless\n");
} */


